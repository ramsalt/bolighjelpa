<?php

/**
 * @file
 * Declare the view API in use.
 */

/**
 * Implementation of hook_views_api().
 */
function addresses_user_views_views_api() {
  $view = array(
    'api' => 2,
    //'path' => drupal_get_path('module', 'addresses_user_views'), -- this is the default
  );
  return $view;
}

// vim: ts=2 sw=2 et syntax=php
